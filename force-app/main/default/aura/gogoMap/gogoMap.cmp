<aura:component controller="MyAppointmentMapController" implements="forceCommunity:availableForAllPageTypes" access="global">
    <aura:attribute name="points" type="Integer" default="0"/>
    <ltng:require styles="{!$Resource.gogomapbox + '/mapbox.css'}"

        scripts="{!$Resource.gogomapbox + '/mapbox.js'}"
afterScriptsLoaded="{!c.doInit}" 
       />
   <!--   <aura:handler name="init" value="{!this}" action="{!c.doInit}"/> -->
	         <div class="slds-grid" style="height:200px">

                 <div class="slds-col">
       <div id="map" style="height:200px">
   
                     </div></div></div>

</aura:component>